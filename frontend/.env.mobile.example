# Used ONLY when building the Android APK.
# Copy to .env.mobile and set your VPS address, then build with:
#   npm run build -- --mode mobile
# (Vite loads .env.mobile automatically for --mode mobile)
VITE_API_URL=http://YOUR_VPS_IP:3000
